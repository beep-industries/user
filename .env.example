# Database Configuration (PostgreSQL for user service)
DATABASE_URL=postgres://userservice:userservice_password@localhost:5432/userservice_db

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
HEALTH_PORT=3001

# Keycloak Configuration
# Note: Keycloak runs on port 8080, so user service uses port 3000
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=myrealm
KEYCLOAK_CLIENT_ID=user-service
KEYCLOAK_CLIENT_SECRET=ABvykyIUah2CcQPiRcvcgd7GA4MrEdx4
KEYCLOAK_INTERNAL_URL=http://client-keycloak-1:8080 # Internal URL for Docker Compose networking

# JWT Configuration
JWT_SECRET=your-jwt-secret-key

POSTGRES_DB=user_db
POSTGRES_USER=user_db_user
POSTGRES_PASSWORD=user_db_user_password
